# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# LLM backends
ollama>=0.2.0                          # local Ollama client

# GCP / Vertex AI (install only on GCP)
# google-cloud-aiplatform>=1.52.0      # uncomment for GCP deployment

# Dev / testing
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
